apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: test-bucket
  annotations:
    argocd.argoproj.io/hook: PostSync
spec:
  forProvider:
    region: eu-north-1
  providerConfigRef:
    name: default